spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_dev
    username: recipe_dev_user
    password: recipe_dev_user
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
